import { useState } from 'react'; import { ICON_NAMES, Icon, type IconName, type IconSize } from '@maxxuxx/react'; import './examples.css';
export default function IconExample(){const [name,setName]=useState<IconName>('search');const [size,setSize]=useState<IconSize>(24);return <div className="component-demo" data-component-demo="icon"><div className="component-demo__controls"><select value={name} onChange={e=>setName(e.target.value as IconName)}>{ICON_NAMES.map(n=><option key={n}>{n}</option>)}</select><select value={size} onChange={e=>setSize(Number(e.target.value) as IconSize)}>{[16,20,24].map(s=><option key={s}>{s}</option>)}</select></div><div className="component-demo__stage"><Icon label={`Selected ${name} icon`} name={name} size={size}/><div className="component-demo__grid">{ICON_NAMES.map(n=><div className="component-demo__item" key={n}><Icon name={n} size={size}/><span className="component-demo__label">{n}</span></div>)}</div></div></div>}
