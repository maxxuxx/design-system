# @hds/react

Haru Design System의 접근 가능한 React 컴포넌트 패키지입니다.

```tsx
import { Button } from '@hds/react';
import '@hds/tokens/tokens.css';
import '@hds/react/styles.css';

export function Example() {
  return <Button>확인</Button>;
}
```

현재 npm 공개 전 단계이며, 저장소에서는 배포용 ESM·타입 선언·CSS tarball을 검증합니다.
